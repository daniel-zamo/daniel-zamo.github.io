---
title: "Introducción a ulimit (Límites de Recursos en Linux)"
description: "Aprende a usar ulimit para establecer límites soft y hard, temporal y permanentemente, en sistemas Linux."
slug: "ulimit-introduccion"
tags: ["Linux", "LFCS", "sysadmin", "ulimit", "bash", "recursos", "limits.conf"]
---

# 🇪🇸 Introducción a `ulimit` en Linux

El comando `ulimit` es una herramienta fundamental para **administradores de sistemas Linux**. Permite establecer y consultar límites de recursos por **usuario** y **sesión**, evitando que procesos individuales consuman excesiva memoria, archivos o CPU, lo que podría comprometer la estabilidad del sistema.

Existen dos tipos de límites:

- **Soft limit** (blando):  
  El valor que se aplica por defecto a un proceso. Puede ser elevado hasta el hard limit por el propio usuario.

- **Hard limit** (duro):  
  El valor máximo que puede establecerse. Solo el superusuario (root) puede incrementarlo.

---

## Ver límites actuales

```bash
ulimit -a
```

## Establecer límites temporales (por sesión)

Para cambiar un límite en la sesión actual:

```bash
ulimit -n 4096      # Cambia el número máximo de archivos abiertos
ulimit -u 2048      # Cambia el número máximo de procesos
```

⚠️ Estos cambios se pierden al cerrar la sesión.

